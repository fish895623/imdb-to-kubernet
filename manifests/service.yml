apiVersion: v1
kind: Service
metadata:
    name: "a-cc87"
    labels:
        app: "a-cc87"
spec:
    type: LoadBalancer
    ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
      name: http
    selector:
        app: "a-cc87"