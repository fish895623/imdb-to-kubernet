var textarea = document.getElementById("a0").addEventListener(
  "paste",
  function () {
    if (this.value !== "") {
      this.value = this.value + "\n";
    }
  },
  false
);
var textarea = document.getElementById("a1").addEventListener(
  "paste",
  function () {
    if (this.value !== "") {
      this.value = this.value + "\n";
    }
  },
  false
);
var textarea = document.getElementById("a2").addEventListener(
  "paste",
  function () {
    if (this.value !== "") {
      this.value = this.value + "\n";
    }
  },
  false
);
var textarea = document.getElementById("a3").addEventListener(
  "paste",
  function () {
    if (this.value !== "") {
      this.value = this.value + "\n";
    }
  },
  false
);
